name: Pruebas Automatizadas

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: [3.12.5]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Configurar Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Ejecutar pruebas unitarias
      run: |
        pytest tests/ -v --cov=src --cov-report=xml:coverage.xml
    
    - name: Subir cobertura a Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella

  integration-test:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12.5'
    
    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Ejecutar servidor en background
      run: |
        uvicorn src.main:app --host 0.0.0.0 --port 8000 &
        sleep 5
    
    - name: Ejecutar pruebas de integración
      run: |
        pytest tests/test_main.py -v
    
    - name: Verificar endpoints de la API
      run: |
        curl --fail --silent --show-error http://localhost:8000/ || exit 1
        curl --fail --silent --show-error http://localhost:8000/stock/AAPL || exit 1
